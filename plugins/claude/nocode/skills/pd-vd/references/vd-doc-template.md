# .vd.md 模板

Step 6 产出 `.vd.md` 时使用此模板。本文档只承载视觉设计——交互结构 / 行为规格 / 状态覆盖属于 `.ix.md`（pd-ix 产出），此处只引用不复制（分工契约见 `{NOCODE_SKILL_REF}/ix-vd-contract.md`）。

```markdown
# 视觉设计: {title}
> 状态: 草稿
> 作者: {username}
> 日期: {yymmdd}
> IX: {.ix.md 路径}
> 产出标准: 高保真可交互原型（「不包含」清单已告知）
> 交付线: Claude Design（原型 <projectId> + DS <dsProjectId>）/ 本地 HTML
> 设计源: [design-source: claude-design <projectId>] / [design-source: prototype <路径>]
> 样张: styleguide.html 路径

## 视觉参考集
| 竞品 | 页面 | 截图 | 视觉特征 | 来源 |
|---|---|---|---|---|

## 视觉方向
[选定方向: 一句话标签 + 三轴位置 + 参考产品 + 取值来源（决策树走到哪个分支: 沿用已有 / 用户选了 X / agent 按坐标选了 X）]

## Tokens（Step 3a 冻结表）
[八项 schema 全量: 色板 / 字体角色 / 字号刻度 / 间距基 / 圆角 / 阴影 / 控件四态规则 / signature。
每个 token: 变量名 + 值 + 一句语义。下游按名继承, 禁改名]
| 变量 | 值 | 语义 |
|---|---|---|
| --color-primary | #3370FF | 主操作、链接、选中态 |

## Components（Step 3b 清单）
[gap analysis 结果 + 每组件覆盖的变体/控件四态; 复用已有设计系统时标注复用来源]
| 组件 | 服务的交互点 | 变体 | 来源 |
|---|---|---|---|

## 原型清单
[每个 IA 页面/视图的实现位置 + 交互入口, 100% 覆盖无遗漏]
| IA 页面/视图 | 类型 | 实现位置 | 交互入口 |
|---|---|---|---|

## 原型阶段补充决策
[原型实现中新产生的视觉决策（含时序参数定值）登记于此——追加式;
 行为类补充决策回 .ix.md「下游澄清回流」节, 此处只记视觉类]
| 决策 | 依据 | 日期 |
|---|---|---|
| （示例）凭证卡背景恒定暗色 #1F2329 不随主题 | 参考 Perplexity 引用卡, 元信息跳层 | 260715 |

## 验证记录
[Step 5f 写入]
1. 测试方案（5a 审批通过的版本）
2. 页面覆盖矩阵
3. 交互覆盖矩阵
4. PRD 路径走查结果
5. vis-review 视觉 9 维度评审结果
6. vis-review findings 摘要（C/W/S, 升档时含交叉审）
7. Playwright verify-report.json 摘要

## 回流检查（Step 6）
- [ ] 行为类补充决策已回写 .ix.md「下游澄清回流」节
- [ ] 视觉类补充决策已登记上方
- [ ] 原型内 token 名与冻结表逐一一致（零改名）

## 待定项
- [TBD] 问题 1
```
