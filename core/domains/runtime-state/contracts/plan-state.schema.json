{
  "$id": "runtime-state.plan-state",
  "type": "object",
  "required": ["planRef", "provider", "items", "createdAt", "updatedAt"],
  "properties": {
    "planRef": { "type": "string" },
    "provider": { "type": "string" },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "subject", "status"],
        "properties": {
          "id": { "type": "string" },
          "subject": { "type": "string" },
          "description": { "type": "string" },
          "status": { "enum": ["pending", "in_progress", "completed"] },
          "handoff": { "type": "string" }
        },
        "additionalProperties": false
      }
    },
    "createdAt": { "type": "string" },
    "updatedAt": { "type": "string" }
  },
  "additionalProperties": false
}
