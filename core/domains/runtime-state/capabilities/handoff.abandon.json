{
  "id": "state.handoff.abandon",
  "domain": "runtime-state",
  "internal": true,
  "contextBindings": { "sessionId": "sessionId" },
  "inputSchema": {
    "type": "object",
    "required": ["sessionId", "handoffId", "generation", "reason"],
    "properties": {
      "sessionId": { "type": "string" },
      "handoffId": { "type": "string" },
      "generation": { "type": "integer" },
      "reason": { "type": "string" }
    },
    "additionalProperties": false
  },
  "outputSchema": "contracts/handoff.schema.json",
  "fallbackOn": "never",
  "platforms": {
    "claude": { "primary": "claude-plugin-data" },
    "codex": { "primary": "codex-plugin-data" }
  }
}
