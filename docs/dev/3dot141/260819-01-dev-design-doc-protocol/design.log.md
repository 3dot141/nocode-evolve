# Header
- task: dev-design 文档协议优化——功能组结构 + 背景/目标 + Closing overview + 格式规范
- status: active
- type: feat
- phase: 开发
- current: 实施
- createdAt: 2026-08-19T21:30:00+0800
- artifacts:
  - log: ./design.log.md
  - design: ./design.md

# Decisions

## DEC-001
- 描述: 任务分类
- 内容: feat——修订 dev-design 三类文档协议，新增结构要求（功能组层 / 背景·目标 / Closing overview / 格式规范清单），改变 skill 产出物结构。
- 后果: 无
- 过程: 方案展示 + 三分歧拍板（本会话）。
- 引用: [ROUND-001]

## DEC-002
- 描述: 开发半场按功能组组织，骨架全套
- 内容: feat 开发半场以功能链（前后端贯穿，含外部协作前置）为组；每组必含 背景 / 目标 / 全景 / 流程 / N.x 子功能块（接口·伪代码·影响文件）/ 问题 / 影响文件汇总——单一功能组的小任务也全套，不豁免。流程图与问题上移组层；块内流程图仅当块有独立控制流。尾部收尾三段：总览 → 架构（系统级 ASCII）→ 文件（全仓总树 + 新增目录清单）。影响文件三层层级：block 树 → 组汇总 → 总表。
- 后果: 波及 writing.md（block 三件套 + self-check）、三个 closure 检查项措辞、SKILL.md Step 2 block 追加措辞。
- 过程: 260819-02 实战七轮返工实证（割裂看不懂 / 复制逻辑漏 / 没背景目标 / 骨架用户亲列 / 尾部用户亲定）；分歧 1 拍板「全套」。
- 引用: [ROUND-001]

## DEC-003
- 描述: bug / refactor 同构对齐
- 内容: bug 修复半场按故障机制分组（背景=机制+Debug 证据回链 / 目标=修复验收 / 机制流程 before-after ASCII，块内流程图保留——修复机制本身是块级控制流）；refactor After 半场按结构变更组分组（背景引 Before 不重复 / 目标引目标质量与停止条件 / 迁移流程）。两者尾部均加 Closing overview。
- 后果: 31f7801 的对齐方向延续——从「深度对齐」升级为「结构同构」。
- 过程: 方案展示确认。
- 引用: [ROUND-001]

## DEC-004
- 描述: 块级接口空缺显式标注、伪代码仍明确；DES 头表不固化
- 内容: ①无契约变更的子功能块（纯 prompt / 文案改动）接口处显式标注「本块无新增/变更接口」，但伪代码必含且具体（写清改动内容，不因是文案改动而含糊）。②design.md 开头 DES 快表不写入协议——与尾部 coverage 表重复，违反「view 非 Registry」既有原则，保持可选。
- 后果: self-check 增加对应检查。
- 过程: 分歧 2、3 拍板（用户原话「文案改动之类的，显示标注，但是伪代码部分需要明确」「不固化」）。
- 引用: [ROUND-001]

# ROUND

## ROUND-001 — closed
### 背景
260819-02（fx-data-agents 业务任务）的 design.md 开发设计经七轮返工收敛：按端分组被否（割裂）→ jsy 复制前置漏设计 → 每功能补背景/目标（两个 heading）→ 用户亲列骨架（全景/功能全景/流程/功能 N 接口·伪代码·影响文件/汇总）→ 四条格式规范（文件树/代码格式化/流程图 ASCII/全景 ASCII）→ 尾部改总览/架构/文件。现行 dev-design 协议未覆盖任何一条。
### 问题
将实战验证的结构固化为 dev-design 文档协议；三个开放分歧待拍板。
### 方案
方案全文见 design.md；分歧：小组豁免 / 接口空缺处置 / DES 头部快表。
### 回答
用户拍板：1 全套；2 文案改动显式标注但伪代码需明确；3 不固化。形成 DEC-002~004，直接实施。
