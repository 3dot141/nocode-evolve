# model/ 全量审计 — 补齐 agent-about.md / agent-catalog-using.md 之外的剩余文件

> 承接 `model-rewrite-v2-decisions.md`（只覆盖已动过手的两个文件）。这份补齐 `model/` 目录其余内容，做同样的"有效性+置信度→决定"核算。
>
> **重要前提差异**：agent-about.md / agent-catalog-using.md 的决策表有两路独立评审（Codex + subagent）背书，置信度可以打到"高"。**下面这些文件没有经过任何独立评审**，全部是我自己的判断——鉴于本会话已经两次证明我自己的"这条没价值"判断是错的（语气规范整节、方案类型枚举），这里所有"建议删/改"的条目置信度上限就是"中"，且明确标注"需要独立评审确认后才能动手"，不能直接采信执行。

## agent-personal.md（2610 字符，已在目标以内）

逐节过了一遍，**没有发现值得动的内容**——每一节都是具体、可执行、密度已经很高：

| 内容单元 | 有效性/价值 | 置信度 | 决定 |
|---|---|---|---|
| wiki/ 何时检索（5条具体触发场景） | 具体场景枚举，从本次经验看这类枚举通常是真实召回依据，不是装饰 | 中（无独立评审，但类比 agent-about.md 里同类枚举被验证为高价值，模式匹配可信） | 维持不动 |
| maturity 感知（5 档分级信任） | 精确分级，影响引用 wiki 内容时的可信度标注方式 | 中 | 维持不动 |
| 删除护栏节 | **安全关键**——被 PreToolUse hook 的 `personal-deletion-guard` 直接指向这里读取，且防的是不可恢复的数据丢失 | 高（有明确的外部消费方：`agent-catalog-1.md` 的"记忆与沉淀"桶直接指向本节） | **不可压缩，维持完整** |

结论：**这个文件不需要改**，它已经是"该多长就多长、没有多余脂肪"的状态。

## agent-karpathy.md（2558 字符，已在目标以内）

12 条通用工程准则（英文模板）。逐条评估，找到几处**值得独立评审、但我自己不敢下判断**的疑点：

| 内容单元 | 有效性/价值 | 置信度 | 决定 |
|---|---|---|---|
| Rule 1 Think Before Coding | 与 agent-about.md"推理外化"部分重叠（后者更具体：已验证/推断/假设标注机制） | 低（可能是有意的"通用原则+具体实现"分层，也可能是真冗余，我无法判断） | **建议单独走一次评审**，不自行下结论 |
| Rule 5 Use model only for judgment calls | 这条读起来像是"给应用开发提的建议"（什么时候该用 LLM 做判断、什么时候该用确定性代码），不太像"agent 在 nocode 这个插件仓库里该怎么工作"的准则，跟本仓库其它规则的性质不一样 | 低（可能对用户在其他项目里也适用就是通用建议，也可能是模板没有本地化，纯属我猜测） | **建议单独确认这条是否符合当前实际使用场景** |
| Rule 6 Token budgets（per-task 4000 / per-session 30000） | 与本会话实际观测到的用量明显不符——本会话多次派发的 workflow/subagent fleet 单次就消耗几百万 token（如 eval-harness 那次 14,725,880 tokens），且这是插件架构里的常规操作模式，不是异常 | **中**（这个不符可以直接拿本会话的真实 token 消耗数字来对比，是可核实的事实，不是纯猜测） | **怀疑这条规则已经不反映实际实践，建议核实要不要删除或改写**（比如改成"大额派发前明确告知量级"而不是一个和实际脱节的硬数字） |
| Rule 8 Read before you write | 与 agent-about.md"方案类工作全程基于真实代码核对"重叠，后者详细得多（前/中/后三阶段+具体约束句） | 低（同 Rule 1，分层还是冗余无法确定） | 建议评审 |
| Rule 10 Checkpoint after every significant step | 与 catalog-using.md"收口前跑 TaskList 核对"有相似精神，但表达层面不同（一个讲"总结进度"，一个讲"核对task状态"） | 低 | 不建议动，性质不完全一样 |
| 其余 6 条（2/3/4/7/9/11/12） | 没发现明显重叠或过时迹象 | — | 维持不动 |

**这个文件的结论**：不建议现在动手改——上面标"低置信度"的几条本质是"通用模板 vs 项目定制内容"之间可能存在的重叠，这类判断我自己做过两次错误示范（语气规范、方案类型枚举都是我以为"没用"结果被证明有用），所以这里不下"删"的决定，只列出疑点，若要处理应该走跟 agent-about.md 同样的双审流程，而不是我自己拍板。**Rule 6 是例外**——它的问题是"和可核实的实际数据不符"这种事实性问题，不是"有没有价值"这种判断性问题，相对更适合直接处理，但仍建议先跟你确认这条规则最初设定的意图（也许 4000/30000 从来就不是給"派 subagent/workflow" 的场景用的，只是给"主 agent 自己直接工作"的场景设的参照，两者语境不同，我不能排除这种可能）。

## agent-catalog-1.md（2898 字符，生成物，禁手改）

这是从 `rules/manifest.json` 生成的路由分片。**任何内容改动都必须走"改 manifest → 跑 generate.mjs"流程，不能直接 Edit**（`model/AGENTS.md` 明确标注的红线）。

价值审查（不涉及改动方式，只看内容本身）：

| 内容单元 | 有效性/价值 | 置信度 | 决定 |
|---|---|---|---|
| 6 个桶的粗触发+负例+具体 rule 索引 | 这是整个规则路由系统的索引表，每条都指向具体 rule 文件，没有发现空转或指向不存在文件的死链 | 高（结构性内容，逐条核对过路径都存在） | 维持不动 |
| 头部"完整版见 agent-catalog-using.md 不重复"指针 | 正确的防重复设计 | 高 | 维持不动 |

**这个文件没有发现问题**，且不在本次审计的可执行范围内（改动路径是 manifest.json，不是直接编辑这份生成物）。

## model/AGENTS.md + model/README.md（非 SessionStart 注入，纯维护文档，不占用会话 context）

这两个文件**不消耗任何 agent 常驻 context**（只有开发者/agent 主动打开 `model/` 目录做维护时才会被 Read），所以不适用"压缩到多少字符"这个目标，但既然要求"从上到下完全过一遍"，还是发现一个真实问题：

**已核实的过期错误**：仓库根 `/README.md` 第 12 行写"7 个 segment"，第 14-16 行列举的 segment 列表里**没有 `model-catalog-using`**。经核对 `model/README.md` 自己就已经标注了这个漂移（它的第 55 行原文写道"根 README 目前写'7 个 segment'……没有跟着同步……当前是 10 次 inject-rules.sh 调用"）。也就是说这个 bug 已经被自己发现并记录在案，只是没有真的去修根 README.md。

**建议**：这是纯文档修订、机械可修（把根 README.md 第 12-16 行的 segment 列表更新成实际的 10 个），风险为零，随时可以处理，不需要额外评审。

## 汇总

| 文件 | 是否需要动 | 理由 |
|---|---|---|
| agent-personal.md | 不需要 | 已经精简，无冗余 |
| agent-karpathy.md | **建议走独立评审后再决定**，Rule 6（token budget）值得单独核实 | 有几处可能的模板/项目内容重叠疑点，但置信度不够支撑我自己下删除结论 |
| agent-catalog-1.md | 不需要（且改动方式受限于 manifest.json） | 结构性索引内容，未发现问题 |
| 根 `README.md` | **可以直接修**（纯文档订正，零行为风险） | "7个segment"过期，缺 `model-catalog-using`，已被 `model/README.md` 自己标注 |
| model/AGENTS.md、model/README.md 本身 | 不需要 | 维护文档质量高，无冗余，且不占用 context 预算 |

到这里，`model/` 目录七个文件已经全部过了一遍。真正需要继续投入精力的还是最初那两个文件（agent-about.md / agent-catalog-using.md）的 v2 重写——那边有具体、有证据支撑的三十多项要修的点；这里新增的主要是"根 README.md 过期"这一个可以顺手处理的低风险修订，以及"agent-karpathy.md 里的 Rule 6 token budget 疑似过时"这一个值得你确认的问题。
