# model/agent-about.md + agent-catalog-using.md 压缩计划 — 目标 ≤3500 字符

> 计划文档，**尚未执行任何改动**。字符数口径统一用 `wc -m`。

## 为什么不再走自动化 SkillOpt 循环

上一轮 `wf_ed66771e-9f4` 对 `agent-about.md` 用满 100 轮预算，只从 7705 降到 7289（-5.4%）。逐句微调型的编辑在留出集验证下很快撞到"继续删就掉分"的天花板——`totalRejectedRegression: 24` 说明后期大部分尝试被拦下。3500 相对当前 7760 需要砍 55%，微调量级不够，必须做**结构性删除/合并**，不是措辞打磨。这次改为人工定制计划，直接 Edit 执行，执行后用同一份 eval bank 复核一遍，而不是重启一轮盲目的自动化编辑循环。

## agent-about.md：7760 → 目标 3500（需砍 ~4260 字符 / 55%）

| 段落 | 现状 | 目标 | 动作 |
|---|---|---|---|
| 本插件工作模型(架构总览) | 851 | 450 | 4 条 bullet 保留但收紧措辞，删掉举例性从句 |
| 输出规范 | 893 | 300 | **删除"语气规范—工程动词"整节**(含反例/正例表，~350 字符，性质与已删除的"说人话"节相同，冗余)；"输出语言"+"推理外化"各自压缩到核心一句话 |
| 工作方式(zoom-out/方案核对/红蓝军/semble/git习惯 5 小节) | 2253 | 1000 | 全部改成"规则一句 + 触发/不触发一句"的terse格式，删掉举例性铺垫段落；"方案类工作核对"的前/中/后 3 条 bullet 合并成 1 段 |
| 用户协作(偏离授权/离场信号/AskUserQuestion) | 1972 | 850 | **删除两处 ❌/✅ worked example**(偏离授权节 ~250 字符、AskUserQuestion 节 ~300 字符)；三条规则本文各自压缩到核心约束，去掉重复措辞 |
| 全局占位符 | 1384 | 700 | **`{username}`/`{NOCODE_SKILL_REF}` 表和"文档产出路径变量"表本体不动**(见下方风险说明)，只删减表前后的解释性句子；"变量解析优先级"压缩成一行 |
| 全局约定 | 389 | 200 | 保留 3 条要点，删除"新增全局约定追加到本文件"这类元提示句 |
| **合计** | **7760** | **~3500** | |

### 风险说明：全局占位符表不能整体挪走

已用 `rg` 核对，`{pd_research_output}` / `{pd_prd_output}` / `{pd_ix_output}` / `{pd_vd_output}` / `{dev_design_output}` / `{dev_plan_output}` 这几个占位符被 `pd-research`/`pd-prd`/`pd-ix`/`pd-vd`/`dev-define`/`dev-design-refine`/`devflow` 的 SKILL.md **直接引用**，这些 skill 内部没有"去读 agent-about.md 表格"这类显式指令——它们默认这份映射表本来就在常驻 context 里。如果把整张表挪到某个 reference 文件只留指针，consuming skill 不会主动去 Read 它，等于**默默断链**（占位符解析不出实际路径）。所以这次计划**只删表周围的说明文字，表本体（6 行路径变量 + 2 行基础占位符）原样保留**。

## agent-catalog-using.md：4808 → 目标 3500（需砍 ~1300 字符 / 27%）

| 段落 | 现状 | 目标 | 动作 |
|---|---|---|---|
| header + 归属说明 | 188 | 120 | 两行 attribution 合并成一行 |
| 触发协议(Step0+fork降级) | 552 | 450 | 保留全部语义，压缩措辞 |
| Skill 调用纪律开头 | 271 | 220 | 保留 |
| Red Flags 表(11 行) | 450 | 280 | 保留最常见的 6 行(问题也是任务/先看代码/不需要正式流程/记得这个skill/杀鸡用牛刀/我在推进啊)，删 5 行低频重复项 |
| 进了skill就走完(4条硬约束+fork例外) | 1089 | 700 | 4 条约束各压缩成 1-2 句，去掉每条后面的补充说明句 |
| 跳步借口表(6行)+❌✅示例 | 778 | 400 | **删除 ❌/✅ worked example**(~250 字符)；借口表从 6 行删到 4 行(合并语义相近的) |
| Skill 类型 | 149 | 140 | 保留 |
| 用户指令优先级 | 203 | 180 | 保留 |
| 何时主动调用/devflow | 408 | 300 | 压缩措辞，触发条件保留 3 条 |
| 何时主动建议 /distill·/sow·/task | 711 | 450 | 3 个 command 的描述各压缩到"一句话说明+一句话触发" |
| **合计** | **4808** | **~3240** | 留 ~260 字符缓冲 |

Red Flags 表和跳步借口表都是"心理借口→现实"映射，两张表语义不同（前者防"跳过触发 skill"，后者防"跳过 skill 内某步"），**不合并**，只各自精简行数。

## 执行后验证

1. 用同一份 eval bank(7 条规则 × train/val = 14 case)对压缩后的两份草稿重跑一遍——不用重启自动化 workflow，人工挑 3-5 个最相关的 eval 场景（`skill-check-before-acting`、`deviate-needs-explicit`、`search-semble` 除外，这条本身有沙箱环境缺陷、无法真实验证）直接问一次，人工判断行为是否还在。
2. **人工逐字通读一遍最终稿**，核对：路径变量表 6 行是否原样保留、有没有残留占位符字面量/语法泄漏（本会话已出过 2 次同类事故）。
3. 确认后按 CLAUDE.md 规则升版本号（`.claude-plugin/plugin.json`，当前 `9.6.0`）。

## 当前状态

计划已写好，**未执行任何 Edit**。等用户确认这份计划（尤其是"删除 3 处 ❌/✅ 示例"和"语气规范整节删除"这两项，属于内容取舍判断，不是纯粹的字数搬运）。
