---
name: refactor-clean
description: 轻量清理型重构：删死代码、清未用依赖、消除重复。用户说「清理/重构/删死代码/清理依赖/去重」时使用。Review 发现冗余代码、Build 中发现碍事代码时也可调用。不处理架构型重构（改设计模式、拆模块走 dev-design）。
---

# refactor-clean：清理型重构

删死代码、清未用依赖、消除重复。不改架构、不加功能——只做减法。

> 通用方法论参考 everything-claude-code v1.2.0 refactor-cleaner agent (MIT)

## 什么时候用

- 用户说"清理一下 / 删死代码 / 清理依赖 / 去重"
- Review 阶段发现冗余代码，回来清理
- Build 中发现已有代码碍事，先清再继续

## 什么时候不用

- 架构型重构（改设计模式、拆分模块、抽接口）→ 走 dev-design
- 正在赶功能、代码库不稳定、没有测试覆盖 → 先稳住再清理
- 不理解为什么代码存在 → 先搞懂再动手

## 流程

### Step 1: 确认测试基线

跑一遍现有测试，确认当前状态是通过的。重构前测试就挂着，后面分不清是你弄坏的还是本来就坏。

```bash
# 项目的测试命令（npm test / go test / pytest 等）
```

测试不过 → 停。先修测试或和用户确认再继续。

### Step 2: 检测

找出要清理的内容。按类型分批，不混着来：

| 类型 | 找什么 | 通用方法 |
|---|---|---|
| 死代码 | 未使用的函数/类/变量 | grep 引用、静态分析工具 |
| 未用 export | 导出了但没人 import | 搜索 import 语句 |
| 未用依赖 | 装了但没用的包 | 包管理器分析 |
| 重复代码 | 做同一件事的多份实现 | 搜索相似函数名/逻辑 |

如果项目是 JS/TS（有 package.json），Read references/js-refactor-tools.md 获取专用工具链。

### Step 3: 审查

工具报的不一定真该删。逐项检查：

- **动态引用**：`import()`/拼接路径/反射调用，工具查不到但在用
- **Public API**：对外接口，内部未用也不能删
- **配置约定**：框架约定文件名、配置引用路径
- **git history**：近期改动可能是在建功能，别删

按风险分级：

| 级别 | 判断标准 | 处理 |
|---|---|---|
| **安全** | grep 无引用、非 public API、非动态导入 | 直接删 |
| **谨慎** | 可能有动态引用、框架约定 | 和用户确认再删 |
| **高风险** | Public API、共享工具函数、近期活跃 | 不删，标记留着 |

### Step 4: 最小删除

一次删一类，每类一个 commit：

1. 先删未用依赖（影响面最小）
2. 再删未用 export / 死函数
3. 再删未用文件
4. 最后合并重复代码（影响面最大）

每删一批立即跑 Step 5，避免多批混删导致出错难定位。

### Step 5: 验证

每批删除后检查 build、测试、有无新增报错；任何一项不过 → 立即回滚这批，定位哪个删错了。

### Step 6: 记录

commit message 写清删了什么函数、为什么判断安全（如 grep -r 无引用、git log 显示最后使用时间），删除量大则在 commit body 列清单。

## 重复代码合并

选测试最全/功能最全/最近维护的一份，改调用方指向它，删其余版本并测试。不要合并出新版本——改得越少越安全。
