# 项目本地 Agent 覆盖

> 覆盖 `nocode-evolve/rules/agent-about.md` 的项目本地配置。优先级最高，会话内同名定义以本文件为准。
> 镜像插件级 agent-about 的四章结构：角色 / 行为准则 / 占位符 / 项目指令。

# 角色配置

继承插件级 `agent-karpathy.md` 四条准则（先思考再写代码 / 简单优先 / 外科手术式改动 / 目标驱动执行），无项目级覆盖。

<!--
如需覆盖某条原则，在此章节明文写出：

> **简单优先**覆盖：本项目允许 monorepo 全局 wrapper 提前抽象 —— 因 X 子项目复用代价高。

否则保留这一行声明即可（不删章节，保持四章对齐）。
-->

# 行为准则

<!--
项目特定的**跨场景**行为偏好。

注意：通用偏好（每个项目都希望生效的，比如「输出文档后 echo 路径」）应放到
插件级 `agent-about.md > 行为准则` 章节，**不要在此处重复**。

如项目暂无特定行为偏好，保留本章节标题即可。
-->

# 占位符覆盖

| 占位符 | 值 | 说明 |
|---|---|---|
| `{username}` | `<TODO: your-name>` | 覆盖默认 `3dot141`。 |

<!-- 按需补充更多占位符。可用占位符清单见插件 agent-about.md。 -->

# 项目指令

> 触发表 —— 按场景路由到 `rules/<topic>.md`。AGENTS.md 只放触发条件，不写细节。

## 提 PR
**触发**：用户说「提 PR / 提交 PR / 创建 PR / push 完想合并」，或显式贴 PR create 链接。
**读**：`rules/pr-create.md`

<!--
添加更多场景，每条一段：

## <场景名>
**触发**：<触发条件，能让 agent 直接判 yes/no，禁止「看情况」「适当时候」类含糊表达>
**读**：`rules/<topic>.md`

触发条件写法见插件 `overlay-agents-personal.md > AGENTS.md 触发条件写法`。
-->
