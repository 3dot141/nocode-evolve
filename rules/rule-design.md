# design 触发

详细协议（含五轴 review、STRIDE threat model、source-driven）见 `skills/design/SKILL.md`。本 rule 仅供 catalog 路由引用。

## 触发条件

用户要求设计方案 / 方案对比 / 架构设计 / 写设计文档 / PRD / RFC / ADR / 重构方案 / 技术选型，或 devflow 路由到 Design 阶段。

## 做法

`Skill(nocode-evolve:design)`
